mutation addAntivirusMetadata($input: AddAntivirusMetadataInputValues!) {
    addAntivirusMetadata(addAntivirusMetadataInput: $input) {
        fileId
        software
        softwareVersion
        databaseVersion
        result
        datetime
    }
}
