query getConsignmentReviewDetails($statusFilter: String) {
    getConsignmentReviewDetails(statusFilter: $statusFilter) {
        consignmentId
        consignmentReference
        reviewStatus
        transferringBodyName
        seriesName
        lastUpdated
    }
}

