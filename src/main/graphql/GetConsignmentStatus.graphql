query getConsignmentStatus($consignmentId: UUID!) {
    getConsignment(consignmentid: $consignmentId) {
        series {
            seriesid
            code
        }
        currentStatus {
            series
            transferAgreement
            upload
            clientChecks
            confirmTransfer
            export
        }
    }
}
