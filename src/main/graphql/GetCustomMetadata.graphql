query customMetadata($consignmentId: UUID!) {
    customMetadata(consignmentid: $consignmentId) {
        name,
        description,
        fullName,
        propertyType,
        propertyGroup,
        dataType,
        editable,
        multiValue,
        defaultValue,
        values {
            value
            dependencies {
                name
            }
        }
    }
}
