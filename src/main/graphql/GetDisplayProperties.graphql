query displayProperties($consignmentId: UUID!) {
    displayProperties(consignmentid: $consignmentId) {
        propertyName
        attributes {
            attribute
            value
            type
        }
    }
}
