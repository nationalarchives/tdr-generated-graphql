query getFileCheckProgress($consignmentId: UUID!) {
    getConsignment(consignmentid: $consignmentId) {
        allChecksSucceeded
        parentFolder
        totalFiles
        files {
            fileStatuses {
                statusType
                statusValue
            }
        }
        fileChecks {
            antivirusProgress {
                filesProcessed
            }
            checksumProgress {
                filesProcessed
            }
            ffidProgress {
                filesProcessed
            }
        }
    }
}
