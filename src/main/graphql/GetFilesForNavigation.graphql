query getFilesForNavigation($consignmentId: UUID!) {
    getConsignment(consignmentid: $consignmentId) {
        files {
            fileId
            fileName,
            fileType
        }
    }
}
