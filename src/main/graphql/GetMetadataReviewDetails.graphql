query getMetadataReviewDetails($consignmentId: UUID!) {
    getMetadataReviewDetails(consignmentid: $consignmentId) {
        metadataReviewLogId,
        consignmentId,
        userId,
        action,
        eventTime
    }
}
